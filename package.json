{
  "name": "railagent",
  "version": "0.1.0",
  "private": true,
  "description": "RailAgent monorepo scaffold for Celo Infra hackathon",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev:api": "pnpm -C apps/api dev",
    "typecheck": "pnpm -r typecheck",
    "test": "node --import tsx --test packages/**/test/**/*.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "@types/node": "^22.10.1"
  }
}
